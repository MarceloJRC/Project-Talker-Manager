// const errorMiddleware = (err, _req, res, _next) => {
//     console.error('Erro middleware', err);
//     res.status(500).json({ message: err.mensage });
// };

// module.exports = errorMiddleware;